module graphTop

//open graph_test
open QuickGraph
open QuickGraph.Graphviz
open QuickGraph.Graphviz.Dot


 //let moduleStr= "module Design("+List.fold() inpL +

    //let graph= new AdjacencyGraph<int, Edge<int>>()
    //let grBiDir= new BidirectionalGraph<string, Edge<string>>()

    //let finSuc=grBiDir.AddVertex(0)
    //let finSuc=grBiDir.AddVertex(1)
    //let finSuc=grBiDir.AddVertex(2)
    //let finSuc=grBiDir.AddEdge(Edge<int>(0,1))
    //let finSuc=grBiDir.AddEdge(Edge<int>(0,2))

    //let graphviz = new GraphvizAlgorithm<int,Edge<int>>(grBiDir)
    //let out=graphviz.Generate()
  
    //let dirOut=grBiDir.ToGraphviz() //let out2=graphviz.Generate(new graphviz.)
    
    //File.WriteAllLines("my_test_graph.dot",[|out|])